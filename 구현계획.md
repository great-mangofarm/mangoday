# 개인 블로그 — 상세 구현 계획

> 확정된 구성(Next.js + Cloudflare + Supabase + Auth.js + BlockNote + PWA) 기준 실행 계획입니다.
> 작업은 **단계(Phase)별로** 진행하며, 각 단계가 끝나면 눈으로 확인 가능한 결과물이 나옵니다.

---

## 0. 기술 스택 최종 정리

| 영역 | 기술 | 비고 |
|------|------|------|
| 프레임워크 | Next.js (App Router) + TypeScript | |
| 스타일 | Tailwind CSS v4 | 모바일 우선 반응형 |
| 디자인 시스템 | **TailGrids (구매본)** `~/Documents/etc/pro-blocks` | 블록·코어 컴포넌트 복사·적용 ★ |
| 호스팅 | Cloudflare (OpenNext 어댑터로 배포) | |
| DB/저장소 | Supabase (Postgres + Storage) | |
| DB 접근 | `@supabase/supabase-js` (HTTP 기반) | Workers에서 바로 동작, 별도 연결장치 불필요* |
| 로그인 | Auth.js (구글·카카오·네이버 — 셋 다 기본 지원) | JWT 세션 방식 |
| 에디터 | BlockNote (글쓰기 화면에 지연 로딩) | |
| 알림 | Web Push + Cloudflare Cron Worker | |
| 차트 | **ApexCharts** (react-apexcharts) | TailGrids 내장 → 디자인 일관성 |
| 캘린더 UI | **FullCalendar** | TailGrids 내장 |
| 앱화 대비 | PWA (manifest + 서비스워커) | 추후 Capacitor 가능 |

### 브랜딩 메모 (나중에 UI 단계에서 적용)
- **메인 컬러: 망고색(amber 계열)** — TailGrids에서 `amber`로 선언된 토큰을 브랜드 메인으로 사용.
- **UI 레퍼런스: 사운드클라우드 / 레딧** 톤 (피드 중심, 깔끔한 카드형).
- 적용 시점: Phase 2(공개 레이아웃) 시작 때 테마 토큰부터 교체.

### 디자인 시스템 활용 방침 (TailGrids)
- 위치: `/Users/everon/Documents/etc/pro-blocks` (Vite 기반 React 19 + Tailwind v4 컴포넌트 세트)
- 방식: npm 패키지가 아니라 **소스 스니펫**이므로, 필요한 블록/컴포넌트를 우리 Next.js 프로젝트로 **복사 후 적용**.
- Next.js 적용 시: 인터랙티브 블록엔 `"use client"` 추가, import 경로 정리.
- 매핑: blog/blog-details → 블로그, signin-signup·social-button → 로그인, dashboard(charts·data-stats·widgets·sidebar) → 관리 대시보드, calendars → 캘린더, forms·file-upload → 일지/업로드, search → 태그검색, marketing(hero·navbar·footers) → 공개 레이아웃, core/* → 기본 UI.

> *정정: 앞서 "Hyperdrive 연결장치 필요"라 했는데, `supabase-js`는 HTTP(REST) 방식이라 Cloudflare에서 추가 장치 없이 바로 됩니다. 더 단순해졌어요. (raw Postgres + ORM을 쓸 때만 Hyperdrive가 필요)

---

## 1. 폴더 구조

```
mangoday/
├─ app/
│  ├─ (public)/                 # 누구나 보는 영역
│  │  ├─ page.tsx               # 공개 메인 (간단 차트 + 최신 글) ★
│  │  ├─ blog/page.tsx          # 블로그 글 목록
│  │  ├─ blog/[slug]/page.tsx   # 블로그 글 상세 (SEO 핵심)
│  │  ├─ tags/[tag]/page.tsx    # 태그별 글 모음 (태그 검색) ★
│  │  ├─ stock/                 # 주식 일지 목록/상세 (공개 토글된 것만)
│  │  ├─ workout/               # 운동 일지 목록/상세 (공개 토글된 것만)
│  │  ├─ calendar/page.tsx      # 공개 캘린더 (공개 토글된 일정만) ★
│  │  └─ layout.tsx
│  ├─ admin/                    # 나만 접근 (글쓰기·관리)
│  │  ├─ layout.tsx             # 인증 가드 (내 이메일만 통과)
│  │  ├─ dashboard/page.tsx     # 관리 대시보드 (전체 통계 차트) ★
│  │  ├─ posts/new/page.tsx     # 새 글 (공개/비공개 토글 포함)
│  │  ├─ posts/[id]/edit/page.tsx
│  │  ├─ journal/               # 주식·운동 일지 입력
│  │  ├─ calendar/page.tsx      # 일정 관리 (반복·공개토글·수행체크)
│  │  └─ comments/page.tsx      # 덧글 관리(숨김 등)
│  ├─ api/
│  │  ├─ auth/[...nextauth]/route.ts
│  │  ├─ comments/route.ts      # 덧글 작성/조회
│  │  └─ push/subscribe/route.ts
│  ├─ sitemap.ts                # SEO
│  ├─ robots.ts                 # SEO
│  └─ manifest.ts               # PWA
├─ components/
│  ├─ editor/BlockNoteEditor.tsx   # dynamic import, ssr:false
│  ├─ comments/CommentSection.tsx
│  ├─ calendar/Calendar.tsx
│  ├─ charts/                      # Recharts 기반 차트 (수행률·포스팅횟수 등) ★
│  ├─ tags/TagFilter.tsx           # 태그 선택·검색 ★
│  └─ journal/StockEntryForm.tsx, WorkoutEntryForm.tsx
├─ lib/
│  ├─ supabase.ts               # 클라이언트 생성
│  ├─ auth.ts                   # Auth.js 설정 (provider, 가드)
│  └─ push.ts                   # 웹푸시 유틸 (VAPID)
├─ workers/
│  └─ notify-cron.ts            # Cloudflare Cron: 알림 발송
├─ public/
│  ├─ sw.js                     # 서비스워커 (푸시 수신)
│  └─ icons/                    # PWA 아이콘
├─ open-next.config.ts          # Cloudflare 배포 설정
└─ wrangler.toml                # Cloudflare 설정 (Cron 포함)
```

---

## 2. 데이터 설계 (Supabase 테이블)

### posts — 블로그 글 + 일지 통합
| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | uuid (PK) | |
| kind | text | `blog` / `stock` / `workout` |
| slug | text (unique) | 블로그 URL용 (SEO) |
| title | text | 제목 |
| content_json | jsonb | BlockNote 원본 (편집용) |
| content_html | text | 렌더된 HTML (표시·SEO용) |
| excerpt | text | 요약 (미리보기·검색용) |
| cover_image_url | text | 대표 이미지 |
| status | text | `draft` / `published` |
| is_public | bool | **공개/비공개 토글** (항목별로 내가 결정) ★ |
| entry_date | date | 일지용 (매매일·운동일) |
| data | jsonb | 종류별 추가 필드 (아래) |
| tags | text[] | 태그 (검색용, GIN 인덱스) ★ |
| published_at | timestamptz | |
| created_at / updated_at | timestamptz | |

- **주식 일지** `data` 예: `{ ticker, side, price, qty, fee, pnl, reason }`
- **운동 일지** `data` 예: `{ bodypart, exercises:[{name, sets, reps, weight}], memo }`
- → 종류가 늘어도 `data`만 확장하면 됨. 통계·차트는 이 필드로 계산.
- **태그 검색**: `tags`에 GIN 인덱스를 걸어 `/tags/[tag]` 페이지에서 빠르게 필터.
- **공개 토글**: 글마다 `is_public`로 노출 여부 결정. 공개 영역은 `is_public=true`만 보여줌.

### comments — 덧글
| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | uuid (PK) | |
| post_id | uuid (FK→posts) | |
| parent_id | uuid (nullable) | 대댓글용 |
| author_name | text | 로그인 표시명 |
| author_provider | text | google/kakao/naver |
| author_key | text | provider+id 해시 (동일인 식별) |
| author_image | text | 프로필 이미지 |
| content | text | 내용 |
| status | text | `visible` / `hidden` |
| created_at | timestamptz | |

### calendar_events — 일정
| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | uuid (PK) | |
| title / description | text | |
| start_at / end_at | timestamptz | |
| all_day | bool | |
| is_public | bool | **공개/비공개 토글** (항목별) ★ |
| is_task | bool | 수행 체크 대상 여부 (반복 습관 등) ★ |
| recurrence | jsonb (nullable) | 반복 규칙 `{ freq, interval, byweekday, until }` ★ |
| notify_at | timestamptz (nullable) | 알림 시각 |
| notified | bool | 발송 완료 표시 |
| color | text | |

### event_completions — 반복일정 수행 기록 ★
| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | uuid (PK) | |
| event_id | uuid (FK→calendar_events) | |
| date | date | 해당 회차 날짜 |
| completed_at | timestamptz | 수행 완료 시각 |

- 반복일정은 회차마다 이 표에 체크. **수행률 = 완료 회차 / 예정 회차**.
- 대시보드의 "반복일정 수행 %"는 이 데이터로 계산.

### push_subscriptions — 푸시 구독 정보
| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | uuid (PK) | |
| endpoint | text | |
| keys | jsonb | p256dh, auth |
| created_at | timestamptz | |

### 보안 (RLS, Row Level Security)
- posts: **읽기는 `published` + `is_public=true`만 공개**, 쓰기/수정은 내 계정(service role)만.
- comments: 읽기 공개, 쓰기는 로그인 사용자, 수정/삭제는 본인 또는 나.
- calendar_events: **읽기는 `is_public=true`만 공개**, 쓰기는 나만. 비공개 일정·수행기록은 관리 영역에서만.
- event_completions / push: 나만 접근.

---

## 3. 작업 순서 (Phase)

각 단계 끝에 **눈으로 확인 가능한 결과**가 나오도록 쪼갰습니다.

### Phase 0 — 프로젝트 세팅
- Next.js + TS + Tailwind 생성
- Supabase 프로젝트 생성, 환경변수 연결
- Cloudflare 배포 파이프라인(OpenNext) 연결, 깃허브 저장소 연동
- ✅ 결과: 빈 페이지가 실제 Cloudflare 주소로 떠 있음

### Phase 1 — 데이터 모델
- 위 테이블 + 인덱스 + RLS 정책 생성
- ✅ 결과: Supabase 대시보드에서 표로 데이터 직접 보고/넣을 수 있음

### Phase 2 — 공개 블로그 + 태그 검색 (SEO 포함)
- 블로그 글 목록(`/blog`) + 글 상세 페이지, ISR로 빠르게
- **태그별 페이지(`/tags/[tag]`) + 태그 필터** (GIN 인덱스로 검색) ★
- `generateMetadata`(제목·요약·OG이미지), sitemap.ts, robots.ts
- ✅ 결과: 더미 글이 검색 최적화 + 태그로 모아보기 됨

### Phase 3 — 관리자 + 글쓰기 (공개 토글 포함)
- ~~Auth.js~~ → **실제 구현: 가벼운 Google OIDC + jose 세션 쿠키** (Auth.js 미사용, Next16/Workers 안정성). admin 영역은 **ADMIN_EMAIL만** 통과
- BlockNote 에디터(지연 로딩)로 글 작성/수정/발행
- **글마다 공개/비공개 토글 + 태그 입력** ★
- 이미지 업로드 → Supabase Storage
- 저장 시 content_json + content_html 둘 다 저장
- ✅ 결과: 내가 로그인해서 노션st로 글 쓰고, 공개여부·태그 지정해 발행

### Phase 4 — 덧글
- 구글·카카오·네이버 로그인
- 덧글 작성/조회/대댓글, 관리자 숨김 기능
- ✅ 결과: 방문자가 소셜 로그인 후 덧글 작성

**구현 현황 (v1 완료 / 일부 미구현):**
- ✅ 소셜 로그인: 구글·카카오 동작 / ⬜ 네이버(자격증명 대기)
- ✅ 덧글 평면(flat) 작성·조회·삭제(본인·관리자), 본문 plain text 렌더(XSS 방지)
- ⬜ **대댓글(답글) 기능 — 미구현. 추후 개발 필요.**
  - DB는 이미 `comments.parent_id` 보유 → 스키마 변경 없이 UI/액션만 추가하면 됨
  - 작업: 답글 작성 폼(부모 id 전달) + 1depth 트리 렌더 + `createComment`에 parentId 인자
- ⬜ 관리자 "숨김(hidden)" 모더레이션 — 현재는 삭제만. `status='hidden'` 토글 추후

### Phase 5 — 테마별 일지
- 주식/운동 입력 폼 + 목록/상세 (공개 토글 포함)
- 간단한 통계(주식 손익 합계, 운동 볼륨 추이 등)
- ✅ 결과: 주식·운동 일지 작성 및 통계 확인

### Phase 6 — 캘린더 (공개 + 반복 + 수행체크)
- 일정 CRUD + 캘린더 UI (모바일 친화)
- **반복일정(매일/매주 등) + 항목별 공개 토글** ★
- **반복 회차 수행 체크**(event_completions 기록) ★
- 공개 캘린더(`/calendar`)는 `is_public` 일정만 노출
- ✅ 결과: 일정 등록/조회 + 습관 수행 체크

### Phase 7 — 대시보드 2종 (차트) ★
- **공개 메인(`/`)**: 방문자용 간단 차트(공개 항목 기준) + 최신 글
- **관리 대시보드(`/admin/dashboard`)**: 전체 통계
  - 반복일정 수행 % (event_completions 집계)
  - 포스팅 횟수 추이 / 종류별 분포
  - 주식 손익·운동 볼륨 요약 등
- Recharts로 시각화
- ✅ 결과: 메인이 차트 대시보드로 동작 (공개/관리 분리)

### Phase 8 — 알림 + PWA
- manifest + 서비스워커, "홈 화면에 추가" 가능
- 웹푸시 구독(VAPID), Cloudflare Cron Worker가 `notify_at` 도래한 일정을 푸시
- ✅ 결과: 폰에 앱처럼 설치 + 일정 시간에 알림 수신

### Phase 9 — 도메인 연결 + 정식 배포
- Cloudflare 도메인 연결(같은 계정이라 간단)
- 프로덕션 배포, 전체 점검
- ✅ 결과: 내 도메인으로 정식 오픈

---

## 4. 배포 방식

- **깃허브 푸시 → Cloudflare 자동 배포** (OpenNext)
- 브랜치마다 **미리보기 URL** 생성 → 실서비스 영향 없이 확인
- 알림용 Cron Worker는 `wrangler`로 함께 배포 (분 단위 스케줄)
- 배포 상태는 Cloudflare 대시보드에서 확인

---

## 5. 미리 준비하면 좋은 것 (사용자)

진행 중 필요해지는 것들 — 미리 알려주시면 매끄럽습니다.
- 각 소셜 로그인 **앱 키 발급**: 구글(Google Cloud), 카카오(Kakao Developers), 네이버(Naver Developers) — 발급 방법은 그때 안내
- **내 관리자 이메일** (글쓰기 권한 부여 기준)
- 블로그 **이름/제목**, 대표 색상 등 (있으면)

---

## 6. 예상 순서 요약

```
Phase 0 세팅 → 1 데이터 → 2 공개블로그+태그검색 → 3 글쓰기(공개토글) →
4 덧글 → 5 일지 → 6 캘린더(반복·수행체크) → 7 대시보드 2종 →
8 알림/PWA → 9 도메인/배포
```

각 Phase는 독립적으로 확인 가능해서, 중간에 우선순위를 바꾸거나 멈춰도 됩니다.
(예: "일지보다 캘린더 먼저" 같은 조정 가능)
