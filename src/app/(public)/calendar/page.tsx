import type { Metadata } from "next";
import { ComingSoon } from "@/components/site/ComingSoon";

export const metadata: Metadata = { title: "캘린더" };

export default function CalendarPage() {
  return <ComingSoon title="캘린더" phase="Phase 6" />;
}
