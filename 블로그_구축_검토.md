# 개인 블로그 구축 — 검토 문서

> 논의를 거쳐 구성이 거의 확정됐습니다. 아래는 최종 결정 + 그 근거 정리본입니다.
> 남은 건 **도메인 연결**뿐이고, 그다음 상세 구현 계획으로 들어갑니다.

---

## 1. 내가 만들고 싶은 것 (요구사항)

- 누구나 볼 수 있지만 **글은 나만** 쓰는 블로그
- 덧글은 **구글 / 카카오 / 네이버** 로그인한 사람이면 달 수 있음 (깃허브 전용 라이브러리는 제외)
- 검색 잘 되게 **SEO** 세팅 필요
- **에디터**가 달린 글쓰기 (이미지·서식 가능)
- **캘린더 + 알림** (일정 관리, 정해진 시간에 알림)
- **테마별 일지** (주식 매매일지, 운동일지 등)
- 도메인은 이미 **Cloudflare**에서 구매함
- 주로 **모바일**에서 사용 예정
- 가능하면 **무료**, 꼭 필요하면 유료도 OK

---

## 2. 최종 구성 (확정)

| 영역 | 결정 | 한 줄 설명 |
|------|------|-----------|
| 프레임워크 | **Next.js** | 사이트 뼈대. SEO·속도 최강 |
| 호스팅 | **Cloudflare** | 무료 한도 넉넉 + 도메인도 여기 있어 한 곳에서 관리 |
| 데이터 저장 | **Supabase** | Postgres. 셀 클릭으로 직접 편집 가능 (← 내가 원한 기능) |
| 이미지 저장 | **Supabase Storage** (또는 R2) | 글에 들어가는 사진 보관 |
| 알림 | **Cloudflare Cron** | 분 단위 무료 스케줄로 푸시 발송 |
| 로그인 | **Auth.js** | 구글·카카오·네이버 연결 |
| 에디터 | **BlockNote** | 노션st 글쓰기. 느리면 Editor.js로 교체 약속 |
| 모바일 | **반응형 웹 + PWA** | 앱처럼 설치·푸시. 나중에 앱 전환 길 열어둠 |
| 비용 | **전부 무료 시작** | 커지면 그때 월 2~4만원 |

---

## 3. 용어 쉽게 이해하기

### "호스팅(Cloudflare)" = 내 사이트를 24시간 켜두는 집주인
내가 만든 사이트는 누군가의 서버에서 항상 돌아가야 접속됩니다. 그 서버를 빌려주는 곳.

### "데이터 저장소(Supabase)" = 글·덧글·일정을 보관하는 창고
화면은 Cloudflare가, 그 안의 **내용물(덧글·일지·일정)** 은 Supabase가 보관.

### "SEO" = 구글 검색 노출 세팅
글 제목·요약·미리보기 이미지를 검색엔진이 잘 읽게 정리. Next.js가 지원.

### "PWA" = 앱처럼 쓰는 웹
"홈 화면에 추가"하면 아이콘 생기고 전체화면 앱처럼 열림 + 푸시 알림. 별도 앱 개발 없이 웹 그대로.

---

## 4. 주요 결정의 근거 (왜 이렇게 정했나)

### 결정 1) 호스팅 = Cloudflare (Vercel 아님)

| | Vercel | **Cloudflare (선택)** |
|---|---|---|
| 설정 난이도 | 쉬움 | 손이 좀 더 가지만 **세팅은 내가 처리** |
| 무료 트래픽 | 월 100GB | **사실상 무제한** |
| 알림(Cron) | 무료는 하루 1번 | **분 단위 무료** |
| 도메인 | 따로 연결 | **이미 여기 있음 → 편함** |

→ 도메인도 Cloudflare에 있고 무료 한도가 더 넉넉해서 Cloudflare로 결정.
→ 세팅 난이도는 내가 흡수하므로 운영 체감 차이 없음.

### 결정 2) DB = Supabase (D1 아님)

핵심 이유: **"DB를 손으로 직접 편집·열람하고 싶다"** 는 요구.

| | **Supabase (선택)** | Cloudflare D1 |
|---|---|---|
| 값 수정 | **셀 클릭 → 바로 수정** | 주로 SQL 명령어 |
| 데이터 보기 | 엑셀처럼 표로 | 덜 다듬어짐 |
| 초보 친화 | ★★★★★ | ★★ |

→ 직접 편집 경험이 Supabase가 압도적. 그래서 호스팅은 Cloudflare, **DB만 Supabase**.
→ 연결 중계(Hyperdrive)는 내가 1회 세팅. "7일 미접속 잠듦"은 매일 쓰면 무관(알림 작업이 매일 접속해 안 잠듦).

### 결정 3) 에디터 = BlockNote (raw Tiptap 아님)

- raw Tiptap = "부품"이라 노션 기능(슬래시 메뉴·드래그·블록)을 직접 다 만들어야 함 → 바퀴 재발명.
- BlockNote = **Tiptap 위에 노션 레이어가 완성된 것**. 확장성은 그대로 유지.
- 무게 걱정: 에디터는 **나만 쓰는 글쓰기 화면에만 지연 로딩** → 방문자 페이지엔 0KB, 독자 속도 영향 없음.
- 타이핑 성능: 속 엔진(ProseMirror)이 빨라서 블로그 글 길이엔 부드러움. 느려지면 **Editor.js로 교체하기로 약속.**

### 결정 4) 비용 = 전부 무료 시작

모든 기능이 무료 구성으로 구현 가능. 유료 전환은 "잘 됐을 때"의 행복한 고민.

#### 트래픽 한도 오해 풀기
- 무료는 **갑자기 요금 청구 안 됨**. 한도 근접 시 알림/제한 방식.
- 이미지는 별도 저장소에 둬서 트래픽 절약. 폭증하면 그때 유료(월 $20~25)로 10배 확장.

### 결정 5) 모바일 = 반응형 웹 + PWA

- 지금은 웹으로 만들되 Capacitor로 앱화하기 좋게 설계.
- 나중에 앱 필요하면: **Capacitor로 웹을 그대로 감싸기(쉬움, 1~2일)** 또는 React Native 재작성(어려움).

---

## 5. 기능별 구현 방향

- **덧글 로그인**: Auth.js로 구글·카카오·네이버 연결. 덧글은 Supabase 저장. 글쓰기는 나만, 덧글은 로그인한 누구나.
- **SEO**: Next.js가 글마다 검색용 정보·사이트맵 자동 생성.
- **에디터**: BlockNote (노션st), 글쓰기 화면에만 지연 로딩. 나만 접근.
- **캘린더 + 알림**: 캘린더 화면 + Cloudflare Cron(분 단위 무료)으로 정해진 시간에 푸시. iPhone도 설치형 PWA면 푸시 받음.
- **테마별 일지**: 글 데이터에 "종류"(블로그/주식/운동) 표시 추가. 주식이면 종목·매수가, 운동이면 부위·중량 칸을 더 둠. 코드 하나로 전부 커버.

---

## 6. 남은 것 — 도메인 연결

- 도메인을 **Cloudflare에서 구매** → 같은 Cloudflare에 호스팅하므로 연결이 가장 단순 (클릭 몇 번).
- 실제 연결은 구현 단계에서 함께 진행.

---

## 7. 결정 체크리스트

- [x] 호스팅: **Cloudflare**
- [x] DB: **Supabase** (직접 편집 위해)
- [x] 에디터: **BlockNote** (느리면 교체)
- [x] 비용: **무료 시작**
- [x] 모바일: **PWA + 앱 전환 길 열어둠**
- [x] 도메인: **Cloudflare 구매 확인됨**

→ 구성 확정. 다음 단계는 **상세 구현 계획**(폴더 구조, 데이터 설계, 작업 순서).

---

*이 구성이면 초기 비용 0원으로 모든 기능 구현 가능하고, 규모가 커지거나 앱이 필요해져도 막히지 않습니다.*
